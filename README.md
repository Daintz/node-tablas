#NOTAS:
```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -n, --number   Es el numero de la tabla de multiplicar     [number] [required]
  -l, --list     Lista la tabla de multiplicar
                                           [boolean] [required] [default: false]
  -p, --pending  Lista la tabla de multiplicar [number] [required] [default: 10]
```